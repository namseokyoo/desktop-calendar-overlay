# Codex Goal Prompt — v0.8.0 Developer/Test Release

## Paste into Codex `/goal`

You are implementing the `v0.8.0` milestone for `/Users/namseokyoo/projects/desktop-calendar-overlay`.

### Mission

Make the current v0.7.1 WPF desktop calendar overlay safe and honest as a developer/tester Google Calendar release. Keep BYO Google Desktop OAuth JSON. Do not attempt official public OAuth yet.

### Branch/tag

- Work branch: `release/v0.8.0-developer-test`
- Target tag after gates pass: `v0.8.0`

### Scope

Implement only v0.8 items:

1. README/OAuth docs clearly say Google sync is `developer/tester mode` and requires local Desktop OAuth JSON at `%LOCALAPPDATA%\DesktopCalendarOverlay\google-oauth-client.json`.
2. Settings UI clearly shows auth state:
   - Mock mode: no OAuth JSON found.
   - Ready to connect: OAuth JSON found, not connected.
   - Connected: Google Calendar sync enabled.
3. Disconnect deletes local token/cache state and returns to mock/fallback mode after refresh/restart.
4. Google Calendar create/edit/delete failures show user-readable errors and do not crash the app.
5. Release docs/checklist for v0.8 exist.
6. Create an execution report.

### Out of scope

- No official OAuth client mode.
- No backend.
- No service account.
- No repeat events.
- No attendee/invitation workflow.
- No installer/MSIX/auto-update.

### Required files

Create/update as needed:

- `README.md`
- `docs/OAUTH_AND_SECURITY.md`
- `docs/RELEASE_NOTES_v0.8.0.md`
- `docs/QA_CHECKLIST_v0.8.0.md`
- `docs/reports/v0.8.0-codex-execution-report.md`
- likely app files:
  - `src/DesktopCalendarOverlay/SettingsWindow.xaml`
  - `src/DesktopCalendarOverlay/SettingsWindow.xaml.cs`
  - `src/DesktopCalendarOverlay/ViewModels/MainViewModel.cs`
  - `src/DesktopCalendarOverlay/Services/GoogleCalendarService.cs`
  - `src/DesktopCalendarOverlay/Services/CalendarServiceRouter.cs`
  - `src/DesktopCalendarOverlay/Services/AppDiagnostics.cs`

### Measurable gates

Pass or honestly mark blocked:

- `README.md` contains `developer/tester mode`.
- Docs contain the exact local JSON path.
- No docs imply normal users can connect without setup.
- Settings exposes the three auth states above.
- Disconnect path deletes token store and resets connected state.
- CRUD failures are caught at UI/ViewModel boundary.
- Logs do not include tokens, OAuth codes, client secrets, auth headers, or full raw event payloads.
- `docs/QA_CHECKLIST_v0.8.0.md` includes manual Windows checks for first launch, mock mode, JSON present, connect, CRUD, disconnect, restart, and release ZIP audit.
- `docs/reports/v0.8.0-codex-execution-report.md` records commands run, files changed, and blocked Windows checks if any.
- `git diff --check` passes.

### Required report format

In `docs/reports/v0.8.0-codex-execution-report.md`, include:

```md
# v0.8.0 Codex Execution Report

## Result
PASS / PARTIAL / BLOCKED

## Completed goals
- ...

## Files changed
- ...

## Gates
| Gate | Result | Evidence |
| --- | --- | --- |

## Commands run
```text
command -> result
```

## Push/tag status
- Branch:
- Tag:
- Pushed: yes/no
- If not pushed, exact pending commands:
```
```
```

### Commit guidance

Commit logical checkpoints:

- `docs: clarify v0.8 developer test release`
- `feat: improve google connection status messaging`
- `fix: harden google disconnect and crud errors`
- `docs: add v0.8 qa and execution report`

Do not tag unless gates pass.
