# Codex Goal Prompt — v0.9.0 Public-Readiness Foundation

## Paste into Codex `/goal`

You are implementing the `v0.9.0` milestone for `/Users/namseokyoo/projects/desktop-calendar-overlay`. Start only after v0.8.0 is committed/tagged or explicitly recorded as complete by Core.

### Mission

Prepare the app for public-release trust without changing it into a backend/SaaS product. Add abstractions, tests, privacy docs, and stronger validation gates.

### Branch/tag

- Work branch: `release/v0.9.0-public-readiness`
- Target tag after gates pass: `v0.9.0`

### Scope

1. Introduce token/credential storage abstraction so Google auth state is not hardwired to one local folder behavior.
2. Introduce OAuth client provider abstraction so BYO JSON and future official app-owned config are swappable.
3. Add a test project for service/model logic that can run with `dotnet test`.
4. Add privacy policy draft and Google OAuth verification prep docs.
5. Improve Windows validation script to include tests and artifact hygiene checks.
6. Create v0.9 release notes, QA checklist, execution report.

### Out of scope

- Do not claim OAuth verification is approved.
- Do not ship official client secrets.
- Do not add a backend broker.
- Do not remove developer BYO JSON unless replaced by a verified official path.

### Required files

Create/update as needed:

- `src/DesktopCalendarOverlay/Services/ITokenStore.cs`
- `src/DesktopCalendarOverlay/Services/LocalGoogleTokenStore.cs`
- `src/DesktopCalendarOverlay/Services/IOAuthClientProvider.cs`
- `src/DesktopCalendarOverlay/Services/LocalJsonOAuthClientProvider.cs`
- `src/DesktopCalendarOverlay/Services/GoogleCalendarService.cs`
- `src/DesktopCalendarOverlay/Services/CalendarServiceRouter.cs`
- `src/DesktopCalendarOverlay/ViewModels/MainViewModel.cs`
- `tests/DesktopCalendarOverlay.Tests/DesktopCalendarOverlay.Tests.csproj`
- `tests/DesktopCalendarOverlay.Tests/CalendarServiceRouterTests.cs`
- `tests/DesktopCalendarOverlay.Tests/SettingsStoreTests.cs`
- `docs/PRIVACY_POLICY_DRAFT.md`
- `docs/GOOGLE_OAUTH_VERIFICATION_PREP.md`
- `docs/QA_CHECKLIST_v0.9.0.md`
- `docs/RELEASE_NOTES_v0.9.0.md`
- `docs/reports/v0.9.0-codex-execution-report.md`
- `scripts/windows-validate.ps1`

### Measurable gates

- Token handling goes through `ITokenStore` or an equivalent narrow interface.
- OAuth client availability is represented explicitly, e.g. missing/local-json/future-official.
- Existing BYO JSON behavior remains supported.
- `dotnet test` exists as a documented gate.
- At least service/model tests exist; avoid fragile WPF UI automation unless already supported.
- Privacy draft truthfully says: local-only token storage, no server upload, user-initiated CRUD only, no selling/sharing/ads use of event data.
- `scripts/windows-validate.ps1` runs restore/build/test and prints artifact hygiene checklist.
- `docs/QA_CHECKLIST_v0.9.0.md` covers first launch, connect, CRUD, disconnect, restart, display scaling, release artifact audit.
- `git diff --check` passes.

### Required report

Create `docs/reports/v0.9.0-codex-execution-report.md` with result, completed goals, files changed, gates, commands run, blocked checks, and push/tag status.

### Commit guidance

- `refactor: isolate google oauth client and token state`
- `test: add desktop calendar service tests`
- `docs: add public readiness privacy and oauth prep`
- `chore: strengthen v0.9 validation gates`

Do not tag unless gates pass.
