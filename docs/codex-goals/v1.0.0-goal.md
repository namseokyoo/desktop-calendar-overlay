# Codex Goal Prompt — v1.0.0 Public Desktop Release

## Paste into Codex `/goal`

You are implementing the `v1.0.0` release milestone for `/Users/namseokyoo/projects/desktop-calendar-overlay`. Start only after v0.95.0-rc1 is complete and Core confirms OAuth/public-release status.

### Mission

Finalize a credible public Windows desktop release. Do not ship v1.0 if normal users still need to create Google Cloud credentials, unless Core explicitly renames the release beta/limited.

### Branch/tag

- Work branch: `release/v1.0.0`
- Target tag after gates pass: `v1.0.0`

### Mandatory precondition

Before tagging v1.0, one of these must be true and documented:

1. Google OAuth verification/public consent path is approved and normal users can connect without manual Google Cloud setup.
2. Core explicitly changes the release label to beta/limited and updates docs accordingly.

If neither is true, stop and report `BLOCKED: public OAuth not ready`.

### Scope

1. Final README/docs pass for public users.
2. Final OAuth/security/privacy alignment.
3. Version metadata update.
4. Final QA checklist and release notes.
5. Final artifact hygiene and smoke test instructions.
6. Execution report with go/no-go status.

### Required files

Create/update as needed:

- `README.md`
- `docs/OAUTH_AND_SECURITY.md`
- `docs/PRIVACY_POLICY_DRAFT.md` or final privacy policy link doc
- `docs/QA_CHECKLIST_v1.0.0.md`
- `docs/RELEASE_NOTES_v1.0.0.md`
- `docs/reports/v1.0.0-codex-execution-report.md`
- `src/DesktopCalendarOverlay/DesktopCalendarOverlay.csproj`
- app metadata/version files if present
- `.github/workflows/windows-release.yml` only if release artifact policy needs correction

### Measurable gates

- README explains install/run from ZIP, supported OS, Google connect/disconnect, privacy/security, and limitations.
- Docs no longer say developer/tester mode as the primary path unless release is explicitly beta/limited.
- OAuth status is truthfully documented.
- App/version metadata is `1.0.0` where applicable.
- `dotnet test` passes on Windows/GitHub Actions.
- `scripts/windows-validate.ps1` passes on Windows/GitHub Actions.
- GitHub release artifact policy remains exactly `desktop-calendar-overlay-win-x64.zip`.
- ZIP audit confirms no OAuth JSON, token store, logs, or event data.
- Clean-machine smoke test instructions exist and are complete.
- `git diff --check` passes.

### Required report

Create `docs/reports/v1.0.0-codex-execution-report.md` with:

- `GO` or `NO-GO`
- OAuth readiness evidence
- docs/version changes
- gates and results
- exact release commands or completed release evidence

### Commit guidance

- `docs: finalize v1.0 public release documentation`
- `chore: set v1.0 release metadata`
- `docs: add v1.0 qa and go-no-go report`

Do not tag v1.0 if public OAuth readiness is not real.
