# Scripts

Scripts in this folder are for Windows-side validation and developer convenience. They are documented here because the current bootstrap host is macOS and cannot validate WPF locally.

Use `windows-validate.ps1` from a Windows machine with the .NET 8 SDK installed. For v0.7.1 release readiness, record manual results against `../docs/QA_CHECKLIST_v0.7.1.md`.
