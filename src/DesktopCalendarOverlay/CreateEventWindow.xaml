<Window x:Class="DesktopCalendarOverlay.CreateEventWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add event"
        Icon="Assets/calendar-app-icon.ico"
        Width="460"
        Height="620"
        MinWidth="420"
        MinHeight="580"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource OverlayBackgroundBrush}">
    <Border Margin="14"
            Padding="22"
            Background="{DynamicResource OverlaySurfaceStrongBrush}"
            BorderBrush="{DynamicResource OverlayBorderBrush}"
            BorderThickness="1"
            CornerRadius="22">
        <StackPanel>
            <TextBlock Text="Add event"
                       Foreground="{DynamicResource OverlayTextPrimaryBrush}"
                       FontSize="28"
                       FontWeight="SemiBold" />
            <TextBlock Text="Create a new event in the selected Google Calendar layer."
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource OverlayTextSecondaryBrush}"
                       Margin="0,6,0,18" />

            <TextBlock Text="Calendar"
                       Foreground="{DynamicResource OverlayTextSecondaryBrush}" />
            <ComboBox x:Name="CalendarLayerBox"
                      DisplayMemberPath="Name"
                      Margin="0,4,0,12" />

            <TextBlock Text="Title"
                       Foreground="{DynamicResource OverlayTextSecondaryBrush}" />
            <TextBox x:Name="TitleBox"
                     Margin="0,4,0,12" />

            <TextBlock Text="Date"
                       Foreground="{DynamicResource OverlayTextSecondaryBrush}" />
            <DatePicker x:Name="EventDatePicker"
                        Margin="0,4,0,12" />

            <CheckBox x:Name="AllDayCheckBox"
                      Content="All day"
                      Foreground="{DynamicResource OverlayTextPrimaryBrush}"
                      Margin="0,0,0,12" />

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Start time"
                               Foreground="{DynamicResource OverlayTextSecondaryBrush}" />
                    <TextBox x:Name="StartTimeBox"
                             Margin="0,4,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="End time"
                               Foreground="{DynamicResource OverlayTextSecondaryBrush}" />
                    <TextBox x:Name="EndTimeBox"
                             Margin="0,4,0,0" />
                </StackPanel>
            </Grid>

            <TextBlock Text="Location"
                       Foreground="{DynamicResource OverlayTextSecondaryBrush}" />
            <TextBox x:Name="LocationBox"
                     Margin="0,4,0,12" />

            <TextBlock x:Name="ErrorText"
                       Foreground="{DynamicResource OverlayAccentBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,12" />

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Style="{StaticResource ChromeButtonStyle}"
                        Margin="0,0,8,0"
                        Click="OnCancelClick" />
                <Button Content="Create"
                        Style="{StaticResource ChromeButtonStyle}"
                        Click="OnCreateClick" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
